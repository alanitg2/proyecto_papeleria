version: '3.8'

services:
  nginx:
    build: .
    ports:
      - "80:80"
    depends_on:
      - node

  node:
    build:
      context: .
      dockerfile: Dockerfile.node
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
